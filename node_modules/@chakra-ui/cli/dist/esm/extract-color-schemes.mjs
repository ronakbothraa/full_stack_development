import { isColorHue } from './utils/is-color-hue.mjs';
import { isObject } from './utils/is-object.mjs';

function extractColorSchemeTypes(theme) {
  const { colors } = theme;
  if (!isObject(colors)) {
    return [];
  }
  return Object.entries(colors).reduce(
    (acc, [colorName, colorValues]) => {
      if (isColorHue(colorValues)) {
        acc.push(colorName);
      }
      return acc;
    },
    []
  );
}

export { extractColorSchemeTypes };
